# SOD2 Menu Automation AI

### Setup
1. Install Python 3.11
2. `py -3.11 -m venv venv`
3. `.\venv\Scripts\Activate.ps1`
4. `pip install -r requirements.txt`

### Usage
- Run `.\venv.ps1` to activate the environment.
- Run `python collect_data.py` to gather images.